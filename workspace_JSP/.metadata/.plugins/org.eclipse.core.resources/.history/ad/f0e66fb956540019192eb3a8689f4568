package sec01.ex01;

import javax.servlet.http.HttpServlet;

//클라이언트가 웹브라우저를 통해서 요청할 서블릿 클래스 만들기
public class FirstServlet extends HttpServlet{ //HttpServlet클래스를 상속 받아야함

	//메소드 오버라이드 단축키 : alt + shift + s + v
	
	
	
}
